---
# Galaxy collections used by roles in this repo.
# Install with: ansible-galaxy install -r requirements.yml
collections:
  - name: community.postgresql
    version: ">=3.0.0"
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
